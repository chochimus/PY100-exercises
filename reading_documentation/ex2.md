In the library reference you can find built-in type str.
This leads to the built in methods where str.lower() can be found. The description of the method shows that it returns a
copy of a given string with all cased characters converted to lowercase.